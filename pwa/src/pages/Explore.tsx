import { Box, Typography } from '@mui/material';

const Explore = () => {
    return (
        <Box sx={{ p: 2 }}>
            <Typography variant="h5">探索页面</Typography>
        </Box>
    );
};

export default Explore; 