import { Box, Typography } from '@mui/material';

const Chat = () => {
    return (
        <Box sx={{ p: 2 }}>
            <Typography variant="h5">聊天页面</Typography>
        </Box>
    );
};

export default Chat; 